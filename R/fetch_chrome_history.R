#!/usr/bin/Rscript
#' fetch Chrome browser history
#'
#' this function will fetch all your Google Chrome browser history and store it
#' in a `chrome_history` psdlow style class with special methods in multiple
#' funcitons defined within this package. Currently, it only reads data from
#' <insert what it does here> while importing that data into a structured class.
#'
#' @export fetch_chrome_history
fetch_chrome_history <- function() {
  message('fetch_chrome_history not implemented')
  # the body of this function has yet to be implemented
  # it needs to read chrome history
  # then export it as a table with attributes needed
}
